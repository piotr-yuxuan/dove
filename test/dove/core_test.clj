(ns dove.core-test
  (:require [clojure.test :refer :all]
            [dove.core :refer :all]))

(deftest api-test
  (testing "TODO" ;; write tests
    ;; I've been testing this code against real-life, proprietary schemas.
    ;; Quite obviously I can't open source them but I should create tests schemas.
    :to-be-released))
